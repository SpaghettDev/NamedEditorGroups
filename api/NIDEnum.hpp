#pragma once

enum class NID : char
{
	GROUP = 1,
	COLLISION,
	COUNTER,
	TIMER,
	EFFECT,
	COLOR,
	DYNAMIC_COUNTER_TIMER,

#ifdef SPAGHETTDEV_NAMED_EDITOR_GROUPS_EXPORTING
	_INTERNAL_LAST,
	_INVALID = static_cast<char>(-1),
	_UNKNOWN = 0
#endif
};
